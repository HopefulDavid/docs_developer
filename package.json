{
  "name": "docs-developer",
  "private": true,
  "packageManager": "pnpm@12.4.0",
  "devDependencies": {
    "git-cliff": "2.13.1"
  },
  "scripts": {
    "changelog:generate": "git-cliff --config cliff.toml --output changelog.md",
    "docs:generate": "node scripts/generate-docs.js",
    "docs:check": "pnpm run changelog:generate && node scripts/generate-docs.js --check",
    "docs:clean": "node scripts/generate-docs.js --clean-artifact _site",
    "docs:compile": "dotnet tool run docfx -- docfx.json --warningsAsErrors",
    "docs:artifact-check": "node scripts/generate-docs.js --verify-artifact _site",
    "docs:build": "pnpm run changelog:generate && pnpm run docs:clean && pnpm run docs:compile && pnpm run docs:artifact-check",
    "docs:serve": "dotnet tool run docfx -- serve _site --hostname 127.0.0.1 --port 4173",
    "lint": "node --check scripts/generate-docs.js && node --check templates/material/public/main.js && node --check tests/canonical-docs.test.js && node --check tests/changelog.test.mjs && node --check tests/generate-docs.test.js && node --check tests/transfer.test.js",
    "test": "node --test --test-isolation=none tests/canonical-docs.test.js tests/changelog.test.mjs tests/generate-docs.test.js tests/transfer.test.js",
    "verify": "pnpm run docs:check && pnpm run lint && pnpm test && pnpm run docs:build"
  },
  "engines": {
    "node": "24.13.0"
  }
}
